import { Heading, Stack } from "@chakra-ui/react"

export const Eventos = () => {
  return (
    <Stack
      borderWidth={'thin'}
      borderColor={'red'}
      alignItems={'center'}
      justifyContent={'center'}
      h={'600px'}
    >
      <Heading>
        Eventos
      </Heading>
    </Stack>
  )
}
