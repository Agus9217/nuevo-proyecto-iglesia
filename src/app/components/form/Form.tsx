import {Heading, Stack} from "@chakra-ui/react";

export const Form = () => {
  return (
    <Stack
      alignItems={'center'}
      justifyContent={'center'}
      h={'600px'}
      borderColor={'cyan'}
      borderWidth={'thin'}
    >
      <Heading>
        Form
      </Heading>
    </Stack>
  )
}